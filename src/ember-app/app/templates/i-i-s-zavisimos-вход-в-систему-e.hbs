<h3 class="ui header">{{t "forms.i-i-s-zavisimos-вход-в-систему-e.caption"}}</h3>

<form class="ui form flexberry-vertical-form" role="form">
  {{flexberry-error error=error}}
  <div class="field">
    <div class="flexberry-edit-panel">
      {{#unless readonly}}
        <button class="ui button save-button" {{action "save"}}>
          {{t "forms.edit-form.save-button-text"}}
        </button>
        {{#unless model.isNew}}
          <button class="ui button save-del-button" {{action "delete"}}>
            {{t "forms.edit-form.delete-button-text"}}
          </button>
        {{/unless}}
      {{/unless}}
      <button class="ui button close-button" {{action "close"}}>
        {{t "forms.edit-form.close-button-text"}}
      </button>
    </div>
  </div>
  <div class="field flexberry-validationsummary-container">
    <div class="sixteen wide">
      {{flexberry-validationsummary errors=(v-get validationObject "messages")}}
    </div>
  </div>
  <div class="field">
    <label>{{t "forms.i-i-s-zavisimos-вход-в-систему-e.датаВхода-caption"}}</label>
    {{flexberry-simpledatetime
      type="date"
      readonly=readonly
      value=model.датаВхода
      class=(if (v-get validationModel "датаВхода" "isInvalid") "error")
      data-test-i-i-s-zavisimos-вход-в-систему-e-датаВхода=true
    }}
    {{flexberry-validationmessage error=(v-get validationModel "датаВхода" "message")}}
  </div>
  <div class="field">
    <label>{{t "forms.i-i-s-zavisimos-вход-в-систему-e.тЧПользователь-caption"}}</label>
    {{flexberry-groupedit
      componentName="тЧПользовательGroupEdit"
      mainModelProjection=modelProjection
      modelProjection=modelProjection.attributes.тЧПользователь
      content=model.тЧПользователь
      readonly=readonly
      orderable=false
      class=(if (v-get validationModel "тЧПользователь" "isInvalid") "error")
      data-test-i-i-s-zavisimos-вход-в-систему-e-тЧПользователь=true
    }}
    {{flexberry-validationmessage error=(v-get validationModel "тЧПользователь" "message")}}
  </div>
</form>
